pipeline {
    agent {
        node {
            label 'maven'
        }
    }

    stages {
        stage('Clone-code1') {
            steps {
                git branch: 'main', url: 'https://github.com/bnz18/devops_trend.git'
            }
        }
    }
}
